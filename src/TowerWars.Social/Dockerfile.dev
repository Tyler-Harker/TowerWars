FROM mcr.microsoft.com/dotnet/sdk:10.0

# Install vsdbg for debugging
RUN apt-get update && apt-get install -y curl unzip \
    && curl -sSL https://aka.ms/getvsdbgsh | bash /dev/stdin -v latest -l /vsdbg \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Copy project files for restore
COPY ["src/TowerWars.Shared/TowerWars.Shared.csproj", "TowerWars.Shared/"]
COPY ["src/TowerWars.Social/TowerWars.Social.csproj", "TowerWars.Social/"]

# Restore dependencies
RUN dotnet restore "TowerWars.Social/TowerWars.Social.csproj"

# Copy source code (volumes will override this)
COPY ["src/TowerWars.Shared/", "TowerWars.Shared/"]
COPY ["src/TowerWars.Social/", "TowerWars.Social/"]

WORKDIR /app/TowerWars.Social

# Expose app and debug ports
EXPOSE 7004 5004

# Run with hot reload
ENTRYPOINT ["dotnet", "watch", "run", "--no-restore", "--urls", "http://0.0.0.0:7004"]
